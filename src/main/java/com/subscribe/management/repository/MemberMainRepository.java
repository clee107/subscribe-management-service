package com.subscribe.management.repository;

import com.subscribe.management.domain.entity.MemberMain;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MemberMainRepository extends JpaRepository<MemberMain, Integer> {
}
